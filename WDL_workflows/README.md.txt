This directory contains WDL workflows for

- Bamindex: Getting *.bai index file from a *.bam file from WGS binary alignment data
- Header_maprate_depth (HMM): Getting the vcf header, overall mapping rate, and mean depth (as ideo-histograms; **only rendered properly on MacOS or similar systems**)

... which can be used on the cloud platform Terra.